{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "proofparcel",
  "scripts": {
    "build": "echo 'Build handled by dfx deploy'",
    "prebuild": "echo 'Prebuild handled by dfx deploy'",
    "pretest": "echo 'Pretest handled by dfx deploy'",
    "start": "echo 'Start handled by dfx deploy'",
    "test": "echo 'Test handled by dfx deploy'"
  },
  "type": "module",
  "devDependencies": {
    "@types/vue": "^1.0.31"
  }
}
